public class percentage {
    public static void main(String[] args) {
        int maxscore = 500;
        int minscore = 450;

        double percentage = (double) minscore / maxscore *100.0d ;

        System.out.println("Student Result is:" + percentage);
    }
}
